window.APP_CONFIG = {
  // Preencha para persistir remotamente no Supabase.
  // Use a URL base do projeto (sem /nome_tabela).
  supabaseUrl: "",
  // Use a ANON KEY completa.
  supabaseAnonKey: "",
  // Pode ser só "demands" (usa schema padrão) ou "schema.tabela".
  tableName: "demands",
  // Schema do Postgres exposto no Supabase (normalmente "public").
  schema: "public",
};
